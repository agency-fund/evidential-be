# HTTP request header name corresponding to the ID of the configuration that the request should use.
HEADER_CONFIG_ID = "Datasource-ID"

# The HTTP request header containing the API key.
HEADER_API_KEY = "X-API-Key"
